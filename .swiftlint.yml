included:
  - Sources
  - Tests
  - UITests
  - Apps
  - scripts/release/render_dmg_background.swift
  - Package.swift

excluded:
  - .build
  - .ai-tmp
  - DerivedData

only_rules:
  - duplicate_imports
